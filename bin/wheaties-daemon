#!/usr/bin/env ruby

begin
  require "daemons"
rescue LoadError => e
  if require "rubygems"
    retry
  else
    raise e
  end
end

Daemons.run File.join(File.dirname(__FILE__), "wheaties-start"),
            :dir_mode => :system,
            :multiple => true
